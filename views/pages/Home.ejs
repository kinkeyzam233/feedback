<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Feedback System</title>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: Arial, sans-serif;
    }

    body,
    html {
      height: 100%;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      color: white;
    }

    /* Background with overlay */
    body {
      background-image: url('/image/college.JPG');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      position: relative;
      overflow-x: hidden;
    }

    body::before {
      content: "";
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background-color: rgba(0, 0, 0, 0.6); /* dark overlay */
      z-index: 0;
    }

    /* Navbar */
    .navbar {
      position: relative;
      z-index: 10;
      background-color: rgba(15, 23, 42, 0.9);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 1rem;
      color: white;
      font-weight: bold;
      font-size: 1.5rem;
      z-index: 10;
    }

    .logo img {
      height: 40px;
      width: auto;
    }

    .nav-links {
      display: flex;
      gap: 1rem;
      z-index: 10;
    }

    .nav-links a {
      color: white;
      text-decoration: none;
      padding: 0.5rem 1rem;
      border-radius: 4px;
      transition: background-color 0.3s;
      font-weight: 500;
    }

    .nav-links a.active,
    .nav-links a:hover {
      background-color: rgba(255, 255, 255, 0.15);
    }

    /* Hero section */
    main.hero {
      position: relative;
      z-index: 5;
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 2rem;
      text-align: center;
      max-width: 900px;
      margin: 0 auto;
      width: 90%;
    }

    .hero-content {
      background-color: rgba(0, 0, 0, 0.5);
      padding: 3rem 2rem;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0, 0, 0, 0.7);
    }

    .hero-content h1 {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .hero-content p {
      font-size: 1.3rem;
      line-height: 1.6;
    }

    /* Footer */
    footer {
      position: relative;
      z-index: 10;
      background-color: rgba(15, 23, 42, 0.9);
      color: white;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .hero-content h1 {
        font-size: 2rem;
      }

      .hero-content p {
        font-size: 1rem;
      }

      .navbar {
        flex-direction: column;
        gap: 1rem;
      }

      .nav-links {
        gap: 0.5rem;
      }
    }
  </style>
</head>

<body>
 <nav class="navbar"> 
  <div class="logo">
    <img src="/image/logo.png" alt="Logo" />
    <span>Feedback System</span>
  </div>
  <div class="nav-links">
    <a href="/" class="active">Home</a>
    <a href="/user/feedback">Feedback</a>
    <% if (!user) { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } else { %>
      <!-- Updated logout link to point to confirmation page -->
      <a href="logout-confirmation.ejs">Logout</a>
    <% } %>
  </div>
</nav>


  <main class="hero">
    <div class="hero-content">
      <h1>Welcome to the Feedback System</h1>
      <p>Your voice matters! Help us improve your educational experience by sharing your thoughts and connecting with
        lecturers.</p>
    </div>
  </main>

  <footer>
    <p>&copy; 2024 College Feedback System. All rights reserved.</p>
  </footer>
</body>

</html>
